# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
*.tsbuildinfo

# React DevTools stubs are now committed to the repository
# (previously generated, now static stubs for production builds)

# Test coverage
coverage/

# Playwright E2E
playwright-report/
test-results/
playwright/.cache/

# Maestro E2E
maestro-debug/

# Android build artifacts
packages/mobile-host/android/app/.cxx/
packages/mobile-host/android/.gradle/
packages/mobile-host/android/build/
packages/mobile-remote-hello/android/app/.cxx/
packages/mobile-remote-hello/android/.gradle/
packages/mobile-remote-hello/android/build/
packages/mobile-remote-hello/android/app/build/

# Android release APKs (build artifacts - ignore all APKs except actual Gradle output)
*.apk
!packages/*/android/app/build/outputs/apk/release/*.apk
!packages/*/android/app/build/outputs/apk/debug/*.apk

# iOS local environment (contains machine-specific paths)
packages/mobile-host/ios/.xcode.env.local
packages/mobile-remote-hello/ios/.xcode.env.local

# iOS build outputs
packages/mobile-remote-hello/ios/build/
packages/mobile-remote-hello/ios/Pods/

# iOS generated bundles and maps (build artifacts)
packages/mobile-host/ios/main.jsbundle
packages/mobile-host/ios/main.jsbundle.map
packages/mobile-remote-hello/ios/main.jsbundle
packages/mobile-remote-hello/ios/main.jsbundle.map

# CocoaPods build configuration cache
packages/mobile-host/ios/Pods/.last_build_configuration
packages/mobile-remote-hello/ios/Pods/.last_build_configuration

# IDE
.vscode/
.idea/
.cursor/
docs/cursor/
*.swp
*.swo
*~

# Backup files
*.bak
*.bak[0-9]
*.bak[0-9][0-9]

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Android release keystore (sensitive - never commit!)
*.keystore
!**/debug.keystore

# Firebase configuration (contains project IDs - keep private)
google-services.json
GoogleService-Info.plist

# Firebase service account keys (NEVER commit - contains private keys!)
*-firebase-adminsdk-*.json

# Firebase cache (generated deployment metadata - should not be committed)
.firebase/

# Yarn
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# Turborepo
.turbo/